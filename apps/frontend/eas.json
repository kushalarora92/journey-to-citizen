{
  "cli": {
    "version": ">= 16.23.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
  "distribution": "internal"
      // No file secret env vars needed; EAS handles file secrets automatically
    },
    "preview": {
  "distribution": "internal"
      // No file secret env vars needed; EAS handles file secrets automatically
    },
    "production": {
  "autoIncrement": true
      // No file secret env vars needed; EAS handles file secrets automatically
    }
  },
  "submit": {
    "production": {}
  }
}
